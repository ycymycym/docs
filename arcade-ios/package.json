{
  "name": "mini-arcade-ios",
  "version": "1.0.0",
  "private": true,
  "description": "Native iOS wrapper (Capacitor) for the Mini Arcade minigame hub — offline play + online game updates.",
  "type": "module",
  "scripts": {
    "prepare:web": "node scripts/prepare-web.mjs",
    "add:ios": "npm run prepare:web && npx cap add ios",
    "sync": "npm run prepare:web && npx cap sync ios",
    "assets": "npx @capacitor/assets generate --ios",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2"
  }
}
